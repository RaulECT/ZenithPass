# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @zenithpass/api-gateway`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t zenithpass-api-gateway`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system zenithpass-api-gateway && \
          adduser --system -G zenithpass-api-gateway zenithpass-api-gateway

COPY apps/api-gateway/dist zenithpass-api-gateway/
COPY apps/api-gateway/package.json zenithpass-api-gateway/
RUN chown -R zenithpass-api-gateway:zenithpass-api-gateway .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix zenithpass-api-gateway --omit=dev -f install

CMD [ "node", "zenithpass-api-gateway" ]
